public class Util {

    public static int CORRECT_OPTION = 0;
    public static int QUIT_OPTION = 1;

    enum MenuOption {
        ADD, ERASE, SELECT, WRITE, QUIT,
        UP, DOWN, LEFT, RIGHT, INCREMENT_SIZE, DECREMENT_SIZE
    }

    enum ShapeOption {
        LINE, CIRCLE, TRIANGLE, RECTANGLE, OVAL, TEXT
    }
}
